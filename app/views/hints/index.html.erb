<div class="form-container">
  <div class="hint-title">
    Listing Hints

    <%= link_to 'New Hint', new_hint_path, class: 'btn btn-primary right' %>
  </div>
  <div class="score_table">
    <table>
      <thead>
        <tr>
        	<th>Rule: </th>
          <th>Hint: </th>
          <th>Explanation: </th>
          <th></th>
          <th></th>
          <th></th>
        </tr>
      </thead>

      <tbody>
        <% @hints.each do |hint| %>
          <tr>
          	<td><%= hint.rule.name %></td>
            <td><%= markdown(hint.body) %></td>
            <td><%= markdown(hint.explanation) %></td>
            <td><%= link_to 'Show', hint %></td>
            <td><%= link_to 'Edit', edit_hint_path(hint) %></td>
            <td><%= link_to 'Destroy', hint, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  <br>

</div>  

